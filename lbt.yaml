name: lbt
modules:
  - name: build
    config:
      targets:
        - { os: linux, arch: amd64 }
        - { os: darwin, arch: amd64 }
        - { os: windows, arch: amd64 }
      commands:
        - cmd/lbt/main.go
      version: true
      ldflags: -s -w
  - name: output
    config:
      module: build
      outDir: out
  - name: version
    config:
      type: buildstr
      path: cmd/lbt/version
